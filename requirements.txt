# Core LLM SDK -- Groq (free tier, no billing required, OpenAI-compatible)
groq>=0.11.0

# Vector database (local, zero-setup -- see architecture_specification.md Section 4.3)
chromadb>=0.5.0

# Config / environment management
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Retry logic for external API calls (Section 4.3 / A4.3 error handling)
tenacity>=8.2.0

# HTTP client (used by SEC EDGAR and other Day 5 tool implementations)
httpx>=0.27.0

# Terminal output formatting for the trace log / dev runs
rich>=13.0.0

# Testing
pytest>=8.0.0

# Sentiment analysis (news_sentiment tool)
textblob>=0.18.0

# Web scraping fallback
beautifulsoup4>=4.12.0

# Financial data (financial_api tool, via yfinance)
yfinance>=0.2.36
pandas>=2.0.0

# Free web/news search, no API key required (web_search, news_sentiment tools)
ddgs>=8.0.0
