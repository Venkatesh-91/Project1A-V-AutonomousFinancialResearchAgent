# Core LLM SDK -- Google Gemini (free API tier, no billing required)
google-genai>=0.3.0

# Vector database (local, zero-setup -- see architecture_specification.md Section 4.3)
chromadb>=0.5.0

# Config / environment management
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Retry logic for external API calls (Section 4.3 / A4.3 error handling)
tenacity>=8.2.0

# HTTP client (used by tool implementations from Day 5 onward)
httpx>=0.27.0

# Terminal output formatting for the trace log / dev runs
rich>=13.0.0

# Testing
pytest>=8.0.0

# Sentiment analysis (Day 5 news_sentiment tool)
textblob>=0.18.0

# Web scraping fallback (Day 5/7 tools)
beautifulsoup4>=4.12.0

# Financial data (Day 5 financial_api tool, optional fallback)
yfinance>=0.2.36
